const COMMENT = require('../sql/comment');
const DYNAMIC = require('../sql/dynamic');
const GROUP = require('../sql/group');
const MESSAGE = require('../sql/message');
const RELATION = require('../sql/relation');
const USER = require('../sql/user');
const QUERY = require('../sql/query');

module.exports = {
    MESSAGE,
    RELATION,
    USER,
    GROUP,
    DYNAMIC,
    COMMENT,
    QUERY,
};
