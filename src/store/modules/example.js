const example = {
    state: () => ({}),
    getters: {},
    mutations: {
        // fn(state, payload) {
        // }
    },
    actions: {
        // init({ commit }) {
        //   // async things
        // }
    },
};

export default example;
